name: CI/CD Pipeline

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # Docker requires lowercase image names
  IMAGE_NAME: ${{ github.repository_owner }}/forgev3

jobs:
  # ===========================================================================
  # Lint and Type Check
  # ===========================================================================
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: forge-cascade-v2/requirements.txt

      - name: Install Python dependencies
        run: |
          cd forge-cascade-v2
          # Install base + dev deps only for lint (skip ML for speed)
          pip install -r requirements-base.txt
          pip install -r requirements-dev.txt

      - name: Run Ruff linter
        # SECURITY FIX (Audit 4): Don't ignore lint failures
        run: |
          cd forge-cascade-v2
          ruff check forge/ --ignore E501,F401

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: forge-cascade-v2/frontend/package-lock.json

      - name: Install frontend dependencies
        run: |
          cd forge-cascade-v2/frontend
          npm ci

      - name: Run ESLint
        # SECURITY FIX (Audit 4): Don't ignore lint failures
        run: |
          cd forge-cascade-v2/frontend
          npm run lint

      - name: TypeScript type check
        run: |
          cd forge-cascade-v2/frontend
          npx tsc --noEmit

  # ===========================================================================
  # Backend Tests
  # ===========================================================================
  test-backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    needs: lint

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: forge-cascade-v2/requirements.txt

      - name: Install dependencies
        run: |
          cd forge-cascade-v2
          # Install base dependencies (fast)
          pip install -r requirements-base.txt
          # Install ML with CPU-only PyTorch (much faster than GPU version)
          pip install -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu
          # Install dev dependencies
          pip install -r requirements-dev.txt

      - name: Run unit tests
        env:
          REDIS_URL: redis://localhost:6379
          JWT_SECRET_KEY: test-secret-key-for-ci-pipeline-min-32-chars
          NEO4J_URI: ${{ secrets.NEO4J_URI }}
          NEO4J_USERNAME: ${{ secrets.NEO4J_USERNAME }}
          NEO4J_PASSWORD: ${{ secrets.NEO4J_PASSWORD }}
        run: |
          cd forge-cascade-v2
          python -m pytest tests/ -v --tb=short || echo "No unit tests found"

      - name: Start test servers
        env:
          JWT_SECRET_KEY: test-secret-key-for-ci-pipeline-min-32-chars
          NEO4J_URI: ${{ secrets.NEO4J_URI }}
          NEO4J_USERNAME: ${{ secrets.NEO4J_USERNAME }}
          NEO4J_PASSWORD: ${{ secrets.NEO4J_PASSWORD }}
          SEED_ADMIN_PASSWORD: TestAdminPassword123!
        run: |
          cd forge-cascade-v2

          # Start Compliance API
          python run_compliance.py &

          # Start Virtuals API
          python run_virtuals.py &

          # Wait for servers to start
          sleep 5

          # Health checks
          curl -f http://localhost:8002/health || exit 1
          curl -f http://localhost:8003/health || exit 1

      - name: Run API tests
        # SECURITY FIX (Audit 4): Don't ignore test failures - make them visible
        # Test the APIs that are actually running (Compliance on 8002, Virtuals on 8003)
        # The full UI integration test requires the main API + frontend which aren't started here
        env:
          SEED_ADMIN_PASSWORD: TestAdminPassword123!
        run: |
          cd forge-cascade-v2
          # Test Compliance API endpoints
          echo "Testing Compliance API..."
          curl -f http://localhost:8002/health || exit 1
          curl -f http://localhost:8002/api/v1/compliance/frameworks || exit 1

          # Test Virtuals API endpoints (note: FastAPI routes have trailing slash)
          echo "Testing Virtuals API..."
          curl -f http://localhost:8003/health || exit 1
          curl -fL http://localhost:8003/api/v1/agents/ || exit 1

          echo "API tests passed!"

  # ===========================================================================
  # Frontend Tests & Build
  # ===========================================================================
  test-frontend:
    name: Frontend Build
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: forge-cascade-v2/frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd forge-cascade-v2/frontend
          npm ci

      - name: Build frontend
        env:
          VITE_API_URL: http://localhost:8001/api/v1
        run: |
          cd forge-cascade-v2/frontend
          npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: forge-cascade-v2/frontend/dist/
          retention-days: 7

  # ===========================================================================
  # Docker Build
  # ===========================================================================
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - name: cascade-api
            context: ./forge-cascade-v2
            dockerfile: ./forge-cascade-v2/Dockerfile
          - name: compliance-api
            context: ./forge-cascade-v2
            dockerfile: ./forge-cascade-v2/Dockerfile
          - name: virtuals-api
            context: ./forge-cascade-v2
            dockerfile: ./forge-cascade-v2/Dockerfile
          - name: frontend
            context: ./forge-cascade-v2/frontend
            dockerfile: ./forge-cascade-v2/frontend/Dockerfile

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.name }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VITE_API_URL=http://localhost:8001/api/v1

  # ===========================================================================
  # Security Scan
  # ===========================================================================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.event_name == 'push'

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # SECURITY FIX (Audit 3): Pin to specific version instead of @master
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

  # ===========================================================================
  # Deploy (Manual Trigger)
  # ===========================================================================
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [docker-build, security-scan]
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "🚀 Deployment triggered for commit ${{ github.sha }}"
          echo "Images available at: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo ""
          echo "To deploy manually:"
          echo "  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/cascade-api:latest"
          echo "  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/compliance-api:latest"
          echo "  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/virtuals-api:latest"
          echo "  docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/frontend:latest"

      # Add your deployment steps here, for example:
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.DEPLOY_HOST }}
      #     username: ${{ secrets.DEPLOY_USER }}
      #     key: ${{ secrets.DEPLOY_KEY }}
      #     script: |
      #       cd /opt/forge
      #       docker-compose pull
      #       docker-compose up -d
